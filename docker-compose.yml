version: "3.7"
services:
  app:
      build:
        args:
          user: aji
          uid: 1000
        context: ./
        dockerfile: ./Dockerfile
      image: hootka
      container_name: hootka-app
      working_dir: /var/www/
      volumes:
        - ./:/var/www
        - ./user.ini:/usr/local/etc/php/php.ini
        - "/etc/timezone:/etc/timezone:ro"
        - "/etc/localtime:/etc/localtime:ro"
      networks:
        - hootka
  nginx:
    image: nginx:1.17-alpine
    container_name: hootka-nginx
    ports:
      - 8000:80
    volumes:
      - ./:/var/www
      - ./default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - hootka
  nodejs:
    image: node:16-alpine
    container_name: hootka-node
    ports:
      - 5173:5173
    volumes:
      - ./:/var/www
    working_dir: /var/www
    entrypoint: ["npm","run", "dev"]
    mem_limit: 512m
    mem_reservation: 128M
    cpus: 0.5
    networks:
      hootka:
        ipv4_address: 10.10.0.100
  # db:
  #   image: mariadb:latest
  #   container_name: hootkadb
  #   ports:
  #     - 3306:3306
  #   volumes:
  #     - ./database:/database
  #   environment:
  #     MYSQL_ROOT_PASSWORD: password
  #   networks:
  #     hootka:
  #       ipv4_address: 10.6.0.99
networks:
  hootka:
    driver: bridge
    ipam:
      driver: default
      config:
          - subnet: 10.10.0.0/24
            gateway: 10.10.0.1
